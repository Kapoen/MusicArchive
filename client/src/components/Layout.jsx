import React from "react";

export default function Layout({ children }) {
    return (
        <main>
            <h1>
                WOW
            </h1>
            {children}
        </main>
    );
};